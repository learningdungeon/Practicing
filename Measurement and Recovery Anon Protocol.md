## Step 4: Measurement and Recovery

1. Everyone applies **Hadamard (H)** gate
2. Everyone measures → bits m₁, m₂, m₃
3. Everyone shares their bits
4. Compute XOR: m₁ ⊕ m₂ ⊕ m₃
5. If XOR = 0 → secret bit 0
   If XOR = 1 → secret bit 1
